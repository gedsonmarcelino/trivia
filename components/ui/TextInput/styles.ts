import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
  default: {
    borderColor: '#E2E2E1',
    borderWidth: 2,
    backgroundColor: "#fff",
    paddingVertical: 14,
    paddingHorizontal: 30,
    borderRadius: 8,
  },
});